---
name: descriptor
description: descriptor

credentials:
  - name: provider

include:

provision:
  - name: {{ asset_name  }}-{{ other_name }}
    provider:
      name: openstack

orchestrate:

execute:

report:

notifications:

