---
name: descriptor
description: descriptor

credentials:
  - name: provider

include:

provision:
  - name: {{ asset_name  }}-{{ other_name }}
    provisioner: openstack

  - name: {{ list_var  }}
    provisioner: openstack

  - name: {{ dict_var  }}
    provisioner: openstack

orchestrate:

execute:

report:

notifications:

